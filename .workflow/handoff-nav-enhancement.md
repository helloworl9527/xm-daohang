# 交接单 · 导航站增强 → Codex UI/UX 设计师

- 交接时间：2026-08-10
- 交接方：Claude 项目架构师（lead）
- 接收方：Codex UI/UX 设计师
- 阶段：ui_prototype
- 项目：收藏系统 · 导航站增强（增量特性）

## 已完成
- 需求探索完成，PRD 定稿并经用户原文「确认需求」（2026-08-10）。
- 事实源：`.workflow/requirements-nav-enhancement.md`（已确认）。
- 基础代码：`xm-daohang/`（Next.js 15 + React 19 + Drizzle + PostgreSQL/pgvector + next-intl 双语），现有公开端为「问答优先 + 每日 3 条轮换」。

## 需要 UI/UX 设计师产出（ui-spec + 可运行原型）
目标：在现有公开首页基础上新增「导航站」形态，四区块同页并存，产出 `.workflow/ui-spec-nav-enhancement.md` 与可运行原型 + 验证证据。

必须覆盖的界面与约束：
1. **公开首页并存布局**：导航目录 + 推荐栏（每日 3 条轮换，保留现状）+ AI 搜索入口（复用现有问答，行为不变）+ **底部提示框（必须保留现状位置与内容）**。
2. **导航目录**：按固定分类分组；每组内**站点卡片按名称排序**；「未分类」分组固定排末尾；**空分类仍显示分组标题**。卡片字段：**favicon 图标 + 标题 + AI 中文总结 + 标签 + 外链**；外链 `target=_blank rel="noopener nofollow"`；点击新标签打开。
3. **F208 分类锚点跳转**：顶部或侧边分类导航条，点击滚动定位到对应分类区块。**不做**站内关键词筛选。
4. **管理端分类管理页**：AI 提议分类的初审界面（候选列表可增删改名 → 确认）、分类 CRUD（新增/重命名/删除，删除需二次确认提示其下条目转未分类）、条目上的分类选择器（手动改分类）。
5. 需给出默认/加载/空/成功/失败状态；沿用现有中英文双语与视觉语言；AI 总结/回答保持中文。

## 约束与非目标
- 不改问答式搜索行为；doc（文本/PDF）不进导航目录；不动现有推荐/限流/加密/Telegram；单管理员/单层扁平分类/单主分类。

## 交还条件
- 产出 `.workflow/ui-spec-nav-enhancement.md` + 可运行原型 + 验证证据，交还架构师，待用户原文「确认界面」后再进入实施计划阶段。
